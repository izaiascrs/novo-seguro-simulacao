import React from 'react'
import loading from '../../images/loading.gif';

const Logo = () => (
        <div>
            <img src={loading} alt="loading" />
        </div>
    )

export default Logo;
